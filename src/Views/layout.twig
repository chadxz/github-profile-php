<!DOCTYPE html>
<html lang="en">
<body>
{% if not flash('login') is empty %}
    <ul class="alert alert-danger">
        {% for msg in flash('login') %}
            <li>{{ msg }}</li>
        {% endfor %}
    </ul>
{% endif %}
{% block content %}{% endblock %}
</body>
</html>
